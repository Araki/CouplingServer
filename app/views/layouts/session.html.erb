<% content_for :content do %>
  <div class="bdnavi">
    <ul class="breadcrumb">
      <li>ユーザー管理 &gt;</li>
      <li><%= link_to "セッション一覧", [:admin, :sessions] %> &gt;</li>
      <li><%= yield :title =%></li>
    </ul>
  </div>
  <%= yield %>
<% end %>
<%= render :file => 'layouts/admin' %>
