<% content_for :content do %>
  <div class="bdnavi">
    <ul class="breadcrumb">
      <li>マスタ管理 &gt;</li>
      <% if @hobby.present? %>
        <li><%= link_to "趣味", [:admin, :hobbies] %> &gt;</li>
        <li><%= yield :title =%></li>
      <% else %>
        <li><%= yield :title =%></li>
      <% end %>
    </ul>
  </div>
  <%= yield %>
<% end %>
<%= render :file => 'layouts/admin' %>