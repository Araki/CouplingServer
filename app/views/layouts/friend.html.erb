<% content_for :content do %>
  <div class="bdnavi">
    <ul class="breadcrumb">
      <li>ユーザー管理 &gt;</li>
      <% if @friend.group.present? %>
        <li><%= link_to "ユーザー一覧", [:admin, :users] %> &gt;</li>
        <li><%= link_to "#{@friend.group.leader.profile.nickname}", [:admin, @friend.group.leader] %> &gt;</li>
        <li><%= yield :title =%></li>
      <% else %>
        <li><%= yield :title =%></li>
      <% end %>
    </ul>
  </div>
  <%= yield %>
<% end %>
<%= render :file => 'layouts/admin' %>