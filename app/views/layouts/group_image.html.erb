<% content_for :content do %>
  <div class="bdnavi">
    <ul class="breadcrumb">
      <li>マスタ管理 &gt;</li>
      <% if @group_image.present? %>
        <li><%= link_to "グループイメージ", [:admin, :group_images] %> &gt;</li>
        <li><%= yield :title =%></li>
      <% else %>
        <li><%= yield :title =%></li>
      <% end %>
    </ul>
  </div>
  <%= yield %>
<% end %>
<%= render :file => 'layouts/admin' %>